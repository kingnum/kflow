## MODIFIED Requirements

### Requirement: Repetition model includes inter-round summary
`kflow-shared/repetition-model.md` SHALL include inter-round summary rules alongside existing repetition specifications.

#### Scenario: Repetition model doc updated
- **WHEN** `kflow-shared/repetition-model.md` is read
- **THEN** it SHALL contain a section on inter-round summary format and injection rules
