## Context

3 处文档矛盾需要修复。阈值矛盾已由用户确认方向（统一为 20）。自审模式矛盾是设计文档更新遗漏（07-agent-model.md 已改为重复制，但 kflow-design.md 设计文档的 ASCII 流程图未同步）。内部重复是追加内容时未删除前版。

## Goals / Non-Goals

**Goals:**
- 修复 3 处矛盾，确保全体系一致性
- 所有修改仅限设计文档层面（运行时 SKILL.md 已正确）

**Non-Goals:**
- 不修改运行时 SKILL.md
- 不引入新的规则或机制

## Decisions

### D1: 阈值修复

`02-directory-structure.md` §2.4 修改：
- `功能点 ≤ 30` → `功能点 ≤ 20`
- `功能点 > 30` → `功能点 > 20`

### D2: 自审流程图修复

`kflow-design.md` 设计文档执行流程图中 SELFREV 步骤修改：
```
旧:
│  9. SELFREV   → 10 轮自循环审查（一致性/完备性/可行性/可测性）
│   ├── Round 1-3:  结构性检查（一致性/完备性）
│   ├── Round 4-7:  细节检查（完备性/可行性）
│   └── Round 8-10: 边界检查（可行性/可测性）

新:
│  9. SELFREV   → 10 轮自循环审查（子代理串行 + 重复制）
│   │   每轮子代理独立执行全部四个维度（一致性+完备性+可行性+可测性）
│   │   子代理发现问题 → 直接修复 + 生成自审报告
```

### D3: 删除重复段落

`04-gates-and-transitions.md` §6.9 整段删除。

## Risks / Trade-offs

| 风险 | 缓解 |
|------|------|
| 修改设计文档可能影响后续 Skill 更新 | 修改内容与 SKILL.md 实现对齐，不会产生不一致 |
