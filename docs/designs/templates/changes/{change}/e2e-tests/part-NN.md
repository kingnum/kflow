---
stage: 详细设计
skill: kflow-design
version: 1.0.0
created_at: 2026-05-05
part: {NN}
range: {TC-XXX ~ TC-YYY}
count: {n}
covered_fp: {FP-XXX ~ FP-YYY}
template_for: e2e-tests/part-{NN}.md
---

# E2E 测试分册：Part {NN}

> **所属变更**: {change-name}
> **版本**: 1.0.0
> **创建时间**: {YYYY-MM-DD}
> **本册覆盖范围**: {TC-XXX ~ TC-YYY}（共 {n} 个场景）

---

## 测试场景

### TC-{id}: {场景描述}

#### 基本信息
- **覆盖功能点**: {关联功能点ID列表}
- **覆盖元素**: {元素覆盖树中的路径，如: 📄 /dashboard → 🏗️ 用户信息区 → 🔘 [编辑按钮] → 🎯 click → 编辑弹窗}
- **验证类型**: {功能验证|UI验证|功能+UI验证}
- **优先级**: {P0/P1/P2}
- **前置条件**: {前置条件描述}

#### 页面路径
{起始页面} → {操作1} → {目标页面}

#### 测试步骤与 DOM 验证

| 步骤 | 操作 | 预期页面/状态 | DOM 验证点 | 验证条件 |
|------|------|-------------|-----------|---------|
| 1 | {用户操作描述} | {页面/状态} | `{CSS选择器}` | {验证条件：存在/文本/值/可见} |
| 2 | {用户操作描述} | {页面/状态} | `{CSS选择器}` | {验证条件} |
| 3 | {用户操作描述} | {页面/状态} | `{CSS选择器}` | {验证条件} |

#### 测试数据

| 数据项 | 值 | 说明 |
|--------|---|------|
| {数据项} | {值} | {说明} |

#### 预期结果

| 验证项 | 预期 | 验证方式 |
|--------|------|---------|
| {验证项} | {预期结果} | DOM验证/截图对比/API响应 |

#### 修改后回查

| 变更类型 | 回查范围 | 说明 |
|---------|---------|------|
| 页面UI修改 | 重新检查所有DOM验证点 | {说明} |
| 数据流修改 | 重新执行完整场景 | {说明} |
| 接口变更 | 验证关联接口响应 | {说明} |

---

## 页面数据来源表

> 标注本册覆盖页面中各数据项的来源，用于测试数据准备和回归影响分析。

| 页面 | 数据项 | 来源类型 | 数据来源路径 | 说明 |
|------|--------|---------|-------------|------|
| {页面名} | {数据项} | {后端API/前端静态/配置控制/浏览器存储} | {API路径/配置文件key/Storage key} | {说明} |

**来源类型枚举**：`后端API`、`前端静态`、`配置控制`、`浏览器存储`。如不在枚举范围内，使用"其他"并附加说明。

---

## 配置项变更影响矩阵

> 当配置项值变更时，SHALL 重新执行受影响的 E2E 测试场景以验证功能正确性。

| 配置项名称 | 变更类型 | 变更描述 | 受影响测试场景ID | 影响说明 |
|-----------|---------|---------|----------------|---------|
| {配置项key} | {值变更/新增/删除} | {描述变更内容} | TC-001, TC-003 | {对测试预期的具体影响} |

> 如本册覆盖功能点均无关联配置项，此表可留空并标注"本册无关联配置项"。

---

## 本册修订记录

| 版本 | 日期 | 修订内容 | 触发阶段 |
|------|------|---------|---------|
| 1.0.0 | {YYYY-MM-DD} | 初始版本 | 详细设计 |
