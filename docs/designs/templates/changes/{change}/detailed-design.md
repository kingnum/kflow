---
stage: 详细设计
skill: kflow-design
version: 1.0.0
created_at: 2026-05-06
template_for: detailed-design.md
template_description: 统一详细设计文档模板（变更级），包含系统架构、设计域章节、非功能需求、子变更划分、配置项设计和错误处理设计。由 kflow-design 生成，作为变更级单一设计文档，合并原 architecture.md 和所有子变更设计内容。
---

# 统一详细设计：{change-name}

> **版本**: 1.0.0
> **创建时间**: {YYYY-MM-DD}
> **关联变更**: {change-name}

---

## 一、系统架构

### 技术选型

| 层级 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 前端框架 | {框架} | {版本} | (前后端项目) |
| 后端框架 | {框架} | {版本} | |
| 数据库 | {数据库} | {版本} | |
| 缓存 | {缓存方案} | {版本} | (如有) |

### 模块划分

{模块划分图和说明}

---

## 二、设计域章节

### 2.1 {设计域名称}（如：认证域）

#### 功能点设计

| 功能点ID | 功能点名称 | 数据模型 | 接口 | 核心流程 | 所属子变更 |
|----------|-----------|---------|------|---------|-----------|
| FP-001 | {名称} | {模型引用} | {接口引用} | {流程引用} | {子变更} |

#### 数据模型设计

##### 实体定义

{实体名称}:
  - 属性: {属性列表}
  - 约束: {约束条件}
  - 关系: {与其他实体关系}

##### 数据表设计

{表结构设计，包括字段、索引、约束}

#### 接口设计

| 接口 | 方法 | 路径 | 入参 | 出参 | 关联配置项 | 配置影响说明 | 说明 |
|------|------|------|------|------|-----------|-------------|------|
| {名称} | GET/POST/... | /api/... | {...} | {...} | {配置项名称，无则填"—"} | {配置值变化对该接口行为的影响，无则填"—"} | {...} |

#### 跨子变更接口契约

{本设计域与其他子变更的接口契约定义}

---

### 2.2 {设计域名称}（如：业务域）

{同上结构}

---

## 三、非功能需求 (NFR)

### 3.1 性能需求

| 需求项 | 目标值 | 测量方式 | 适用场景 |
|--------|--------|---------|---------|
| API P95 响应时间 | < {n}ms | 接口单元测试 | 所有查询接口 |
| 页面加载时间 | < {n}s (首次) | E2E测试 | 前后端项目 |
| 数据库查询时间 | < {n}ms | 单元测试 | 复杂查询 |

### 3.2 安全需求

| 需求项 | 要求 | 验证方式 | 适用场景 |
|--------|------|---------|---------|
| 认证机制 | {JWT + Refresh Token / ...} | 安全审查 | 所有接口 |
| 权限控制 | {RBAC 角色权限 / ...} | 安全审查 | 管理接口 |
| 数据加密 | {加密方案} | 安全审查 | 用户个人信息 |
| SQL注入防护 | 参数化查询 | 代码审查 | 所有数据层 |
| XSS防护 | 输出编码 | 代码审查 | 前后端项目 |

### 3.3 可用性需求

| 需求项 | 目标值 | 说明 |
|--------|--------|------|
| 服务可用率 | {99.9%} | 非计划停机 < {n}h/年 |
| 故障恢复时间 | < {n}min | {恢复策略} |

### 3.4 可维护性需求

| 需求项 | 要求 | 说明 |
|--------|------|------|
| 日志规范 | 结构化日志 (JSON) | 包含 traceId |
| 代码覆盖率 | >= {80%} | 接口单元测试 |
| 文档完整性 | 所有接口有 OpenAPI 描述 | |

---

## 四、子变更划分

### 划分结果

| 子变更 | 类型（自动推断） | 功能点数(后端+前端) | 包含功能点ID | 依赖子变更 | 优先级 | 执行类型 |
|--------|----------------|-------------------|-------------|-----------|--------|---------|
| {subchange-1} | 后端子变更 | {n}个后端 + 0个前端 | FP-001~FP-00{n} | 无 | 高 | AFK |
| {subchange-2} | 前端子变更 | 0个后端 + {m}个前端 | FP-00{n+1}~FP-00{n+m} | {subchange-1} | 中 | HITL |

> **类型列说明**：SHALL 由系统自动推断。读取 functional-designs/index.md 中该子变更包含的所有 FP 类型列：
> - 全部「后端」→ 后端子变更
> - 全部「前端」→ 前端子变更
> - 混合 → **拒绝划分**，提示拆分
>
> SHALL NOT 由人工手动填写。

### 依赖关系图

{subchange-1} → {subchange-2} → {subchange-3}

### 实现顺序

1. {subchange-1}（无依赖，优先实现）
2. {subchange-2}（依赖 {subchange-1}）
3. {subchange-3}（依赖 {subchange-2}）

---

## 五、配置项设计

| 配置项 | 类型 | 默认值 | 环境区分 | 说明 |
|--------|------|--------|---------|------|
| {名称} | {类型} | {值} | dev/test/staging/prod | {描述} |

## 六、错误处理设计

| 错误场景 | 错误码 | 处理方式 | 用户提示 |
|----------|--------|---------|---------|
| {场景} | {code} | {处理} | {提示} |

---

## 七、修订记录

| 版本 | 日期 | 修订类型 | 修订内容 | 影响功能点 | 触发阶段 |
|------|------|---------|---------|-----------|---------|
| 1.0.0 | {YYYY-MM-DD} | 初始版本 | 初始版本 | — | 详细设计 |
