---
stage: 详细设计
skill: kflow-design
version: 1.0.0
created_at: 2026-05-05
part: {NN}
range: {API-XXX ~ API-YYY}
count: {n}
template_for: api-tests/part-{NN}.md
---

# 接口测试分册：Part {NN}

> **所属变更**: {change-name}
> **版本**: 1.0.0
> **创建时间**: {YYYY-MM-DD}
> **本册覆盖范围**: {API-XXX ~ API-YYY}（共 {n} 个接口）

---

## 接口测试用例

### API-{id}: {接口名称}

#### 基本信息
- **方法**: {GET/POST/PUT/DELETE/PATCH}
- **路径**: /api/{path}
- **功能点**: {关联功能点ID}
- **认证要求**: {JWT/无/API Key}
- **权限要求**: {角色/权限}

#### 请求结构

| 参数 | 位置 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|------|--------|------|
| {param} | {query/body/path/header} | {type} | {是/否} | {default} | {说明} |

#### 响应结构

**成功响应 (HTTP 200)**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": "{type}",
    "name": "{type}",
    "createdAt": "{type}"
  }
}
```

**错误响应 (HTTP 4xx/5xx)**:
```json
{
  "code": {error_code},
  "message": "{error message}",
  "data": null
}
```

| 错误码 | HTTP状态 | 触发条件 | 说明 |
|--------|---------|---------|------|
| {code} | {status} | {条件} | {说明} |

#### 测试用例

##### Happy Path (HP)

| 用例ID | 用例描述 | 前置条件 | 请求参数 | 预期HTTP状态 | 预期响应关键字段 |
|--------|---------|---------|---------|-------------|---------------|
| ATC-{id}-HP-01 | {描述} | {前置} | {参数} | 200 | data.{field} = {value} |

##### Error Path (EP)

| 用例ID | 用例描述 | 前置条件 | 请求参数 | 预期HTTP状态 | 预期错误码 |
|--------|---------|---------|---------|-------------|-----------|
| ATC-{id}-EP-01 | {描述} | {前置} | {参数} | 400 | {code} |
| ATC-{id}-EP-02 | {描述} | {前置} | {参数} | 401 | {code} |

##### Edge Case (EC)

| 用例ID | 用例描述 | 前置条件 | 请求参数 | 预期HTTP状态 | 预期结果 |
|--------|---------|---------|---------|-------------|---------|
| ATC-{id}-EC-01 | {描述} | {前置} | {参数} | {status} | {结果} |

---

## 本册修订记录

| 版本 | 日期 | 修订内容 | 触发阶段 |
|------|------|---------|---------|
| 1.0.0 | {YYYY-MM-DD} | 初始版本 | 详细设计 |
