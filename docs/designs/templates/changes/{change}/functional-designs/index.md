---
stage: 设计探索
skill: kflow-explore
version: 1.0.0
created_at: 2026-05-05
template_for: functional-designs/index.md
---

# 功能设计索引：{change-name}

> **版本**: 1.0.0
> **创建时间**: {YYYY-MM-DD}
> **变更类型**: {产品需求|功能需求|功能缺陷}
> **项目类型**: {前后端项目|纯后端项目}

---

## 一、变更概述

### 问题描述

{当前存在的问题或用户需求，1-2 段描述}

### 目标用户

{受益用户群体描述}

### 预期价值

{解决的问题和带来的价值}

### 项目类型判断

| 检测项 | 检测结果 | 判定依据 |
|--------|---------|---------|
| 前端框架依赖 | {存在/不存在} | {package.json 中的框架或缺失} |
| 前端源文件 | {存在/不存在} | {.vue/.tsx/.jsx 文件检测结果} |
| 前端构建配置 | {存在/不存在} | {构建配置文件检测结果} |
| **结论** | **{前后端项目/纯后端项目}** | — |

---

## 二、功能点清单

### 逐条清单

| 功能点ID | 名称 | 简述 | 类型 | 优先级 | 关联功能点 | 依赖功能点 | 所在分册 |
|----------|------|------|------|--------|-----------|-----------|---------|
| FP-001 | {名称} | {简述（1句话）} | 后端 | P0 | #002 | 无 | part-01 |
| FP-002 | {名称} | {简述（1句话）} | 前端 | P1 | #001, #003 | #001 | part-01 |
| FP-003 | {名称} | {简述（1句话）} | 后端 | P2 | #002 | #002 | part-01 |

### 统计信息

| 统计项 | 值 |
|--------|---|
| 总功能点数 | {n} |
| 后端 FP 数量 | {backend_n} |
| 前端 FP 数量 | {frontend_n} |
| 分册数 | {m} |
| P0 数量 | {p0} |
| P1 数量 | {p1} |
| P2 数量 | {p2} |
| 跨层关联 FP 对 | {cross_tier_pairs} |

---

## 三、功能结构树

```
{应用名称}
├── 📁 {模块1}（{n}个功能点）
│   ├── FP-001 {功能名} [P0] — {一句话功能简述}
│   ├── FP-002 {功能名} [P1] — {一句话功能简述}
│   │   └── 依赖: FP-001
│   ├── FP-003 {功能名} [P1] — {一句话功能简述}
│   └── FP-004 {功能名} [P2] — {一句话功能简述}
├── 📁 {模块2}（{m}个功能点）
│   ├── FP-005 {功能名} [P0] — {一句话功能简述}
│   └── FP-006 {功能名} [P1] — {一句话功能简述}
└── 📁 其他（{k}个功能点）
    └── FP-xxx {功能名} [P2] — {一句话功能简述}
```

图例:
- `📁` 模块节点（标注功能点数量）
- `[P0]` 核心功能 `[P1]` 重要功能 `[P2]` 辅助功能
- `—` 功能简述（40 字以内）
- `依赖: FP-xxx` 前置依赖关系
- `→` 前置依赖 `||` 并行 `⊃` 包含

---

## 四、核心业务流程图

```
{业务流程名1}:
[触发点] ──▶ [步骤1] ──▶ [步骤2] ──▶ [步骤3] ──▶ [结束]
                │                       │
                └── [分支条件A]           └── [异常: {处理方式}]

{业务流程名2}:
[触发点] ──▶ [步骤1] ──▶ {分支: 条件B}
                              │
                              ├── [路径1] ──▶ [结束1]
                              └── [路径2] ──▶ [结束2]
```

> 每个流程从触发点到最终结果形成完整路径，标注分支条件和异常路径。

---

## 五、功能点依赖关系图

```
FP-001 (无依赖) ──▶ FP-002 (依赖 FP-001) ──▶ FP-004 (依赖 FP-002)
                       │
                       └──▶ FP-003 (依赖 FP-002)
FP-005 (无依赖) ──▶ FP-006 (依赖 FP-005)
```

图例:
- `──▶` 前置依赖: A 必须在 B 前完成
- `││` 并行关系: A 和 B 可同时实现

---

## 六、分册总览

| 分册 | 文件 | 功能点范围 | 功能点数 | 内容简述 |
|------|------|-----------|---------|---------|
| Part 01 | [part-01.md](part-01.md) | FP-001 ~ FP-030 | {n} | {简述} |
| Part 02 | [part-02.md](part-02.md) | FP-031 ~ FP-060 | {m} | {简述} |

---

## 七、配置项影响矩阵

> 汇总本变更所有功能点关联的配置项，供后续阶段（编码、测试）参考。每个配置项 SHALL 标注其影响的全部功能点。

| 配置项名称 | 类型 | 默认值 | 受影响功能点ID列表 | 影响类型汇总 |
|-----------|------|--------|-------------------|-------------|
| {配置项key} | {布尔/字符串/数字/枚举/JSON} | {默认值} | FP-001, FP-003 | {控制可见性, 控制数据范围} |

**影响类型枚举**：`控制可见性`、`控制数据范围`、`控制校验规则`、`控制流程分支`、`控制权限`。如不在枚举范围内，使用"其他"并附加说明。

> 如本变更所有功能点均无关联配置项，此表可留空并标注"本变更无关联配置项"。

---

## 八、修订记录

| 版本 | 日期 | 修订类型 | 修订内容 | 影响功能点 | 触发阶段 |
|------|------|---------|---------|-----------|---------|
| 1.0.0 | {YYYY-MM-DD} | 初始版本 | 初始版本 | — | 设计探索 |
