---
stage: 设计探索
skill: kflow-explore
version: 1.0.0
created_at: 2026-05-05
part: {NN}
range: {FP-XXX ~ FP-YYY}
count: {n}
template_for: functional-designs/part-{NN}.md
---

# 功能设计分册：Part {NN}

> **所属变更**: {change-name}
> **版本**: 1.0.0
> **创建时间**: {YYYY-MM-DD}
> **本册覆盖范围**: {FP-XXX ~ FP-YYY}（共 {n} 个功能点）

---

## 本册功能点列表

| 功能点ID | 名称 | 类型 | 优先级 | 关联功能点 | 依赖功能点 |
|----------|------|------|--------|-----------|-----------|
| FP-{id} | {名称} | {后端/前端} | {P0/P1/P2} | #{id} | #{id} |

---

## 功能点详细定义

### FP-{id}: {功能点名称}

#### 类型
{后端|前端}

> 判定规则见 [functional-designs/index.md](index.md)——后端：API 实现/服务逻辑/数据模型/数据库操作/业务规则/外部服务集成；前端：页面组件/UI 组件/路由配置/状态管理/样式实现/CSS 变量注入。跨层关联标注见下方「关联功能点」字段。

#### 优先级
{P0|P1|P2}

#### 用户故事
> 作为一个 {角色}，我想要 {功能}，以便 {价值}

#### 所属页面与菜单

| 页面名称 | 菜单路径 | 入口条件 |
|---------|---------|---------|
| {页面名} | {一级菜单 > 二级菜单 > 页面} | {进入该页面的前置条件} |

#### 可执行操作

| 操作名称 | 触发方式 | 可见条件 | 操作描述 |
|---------|---------|---------|---------|
| {操作名} | {按钮点击/链接跳转/表单提交/快捷键/手势} | {如"仅管理员可见"、"登录后显示"} | {操作描述} |

#### 表单项定义

> 仅当功能点涉及表单输入时填写

| 字段名 | 显示标签 | 输入类型 | 必填 | 校验规则 | 默认值 | 占位提示 |
|--------|---------|---------|------|---------|--------|---------|
| {field_name} | {显示标签} | {文本框/下拉/日期选择/开关/...} | {是/否} | {格式/长度/取值范围} | {默认值} | {提示文本} |

**表单联动规则**：

| 触发字段 | 联动字段 | 联动行为 |
|---------|---------|---------|
| {字段A} | {字段B} | {如"选择A时B字段显示"} |

#### 业务规则

##### 前置条件

| 编号 | 条件描述 |
|------|---------|
| PRE-{id} | {使用业务语言描述，如"用户已登录"、"购物车非空"} |

##### 校验规则

| 编号 | 校验对象 | 校验条件 | 不符合时提示 |
|------|---------|---------|------------|
| VAL-{id} | {校验对象} | {校验条件} | {用户提示信息} |

##### 触发条件与后置结果

| 触发条件 | 成功后置结果 | 失败后置结果 |
|---------|------------|------------|
| {用户操作/系统事件/定时任务} | {状态变更/通知/后续跳转} | {回滚/错误提示/重试} |

#### 业务流程上下文

| 所属流程 | 流程中的位置 | 上游操作 | 下游操作 |
|---------|------------|---------|---------|
| {流程名称} | {在流程中的步骤位置} | {本功能点之前需要完成的操作} | {本功能点完成后触发的操作} |

#### 功能行为矩阵

| 场景类型 | 场景描述 | 前置条件 | 触发动作 | 预期结果 |
|---------|---------|---------|---------|---------|
| Happy Path (HP) | {正常流程场景} | {前置条件} | {用户操作} | {预期结果} |
| Error Path (EP) | {异常场景1} | {前置条件} | {用户操作} | {错误提示/处理结果} |
| Error Path (EP) | {异常场景2} | {前置条件} | {用户操作} | {错误提示/处理结果} |
| Edge Case (EC) | {边界场景} | {前置条件} | {用户操作} | {预期结果} |

#### 交互约束

| 约束类型 | 约束描述 | 适用场景 |
|---------|---------|---------|
| 输入约束 | {输入字段的格式、长度、范围限制} | {适用场景} |
| 状态约束 | {功能点在特定状态下不可用或行为不同} | {适用场景} |
| 顺序约束 | {必须先完成A再执行B} | {适用场景} |
| 权限约束 | {需要特定角色或权限} | {适用场景} |

#### 关联配置项

> 每个功能点 SHALL 标注其关联的配置项。如无关联配置项，标注"本功能点无关联配置项"。

| 配置项名称 | 关联功能点ID | 关联类型 | 配置值变化→功能行为变化描述 |
|-----------|-------------|---------|--------------------------|
| {配置项key} | FP-{id} | {控制可见性/控制数据范围/控制校验规则/控制流程分支/控制权限} | {描述：配置值从X变为Y时，功能行为的具体变化} |

**关联类型枚举**：`控制可见性`、`控制数据范围`、`控制校验规则`、`控制流程分支`、`控制权限`。如不在枚举范围内，使用"其他"并附加说明。

---

### FP-{id+1}: {功能点名称}

...（同上结构）

---

## 本册修订记录

| 版本 | 日期 | 修订内容 | 触发阶段 |
|------|------|---------|---------|
| 1.0.0 | {YYYY-MM-DD} | 初始版本 | 设计探索 |
