---
stage: 归档
skill: kflow-archive
version: 1.0.0
created_at: 2026-05-17
part: {NN}
range: {FP-XXX ~ FP-YYY}
count: {n}
template_for: docs/designs/functional-designs/{menu}/part-{NN}.md
---

# 功能设计分册：Part {NN}

> **所属模块**: {menu-name}（{menu}）
> **版本**: 1.0.0
> **创建时间**: {YYYY-MM-DD}
> **最后更新**: {YYYY-MM-DD}
> **本册覆盖范围**: {FP-XXX ~ FP-YYY}（共 {n} 个功能点）

---

## 本册功能点列表

| 功能点ID | 名称 | 优先级 | 关联功能点 | 依赖功能点 |
|----------|------|--------|-----------|-----------|
| FP-{id} | {名称} | {P0/P1/P2} | #{id} | #{id} |

---

## 功能点详细定义

### FP-{id}: {功能点名称}

> 来源变更: {change-name} | 归档时间: {YYYY-MM-DD}

#### 优先级
{P0|P1|P2}

#### 用户故事
> 作为一个 {角色}，我想要 {功能}，以便 {价值}

#### 所属页面与菜单

| 页面名称 | 菜单路径 | 入口条件 |
|---------|---------|---------|
| {页面名} | {一级菜单 > 二级菜单 > 页面} | {进入该页面的前置条件} |

#### 可执行操作

| 操作名称 | 触发方式 | 可见条件 | 操作描述 |
|---------|---------|---------|---------|
| {操作名} | {按钮点击/链接跳转/表单提交/快捷键/手势} | {如"仅管理员可见"、"登录后显示"} | {操作描述} |

#### 表单项定义

> 仅当功能点涉及表单输入时填写

| 字段名 | 显示标签 | 输入类型 | 必填 | 校验规则 | 默认值 | 占位提示 |
|--------|---------|---------|------|---------|--------|---------|
| {field_name} | {显示标签} | {文本框/下拉/日期选择/开关/...} | {是/否} | {格式/长度/取值范围} | {默认值} | {提示文本} |

**表单联动规则**：

| 触发字段 | 联动字段 | 联动行为 |
|---------|---------|---------|
| {字段A} | {字段B} | {如"选择A时B字段显示"} |

#### 业务规则

##### 前置条件

| 编号 | 条件描述 |
|------|---------|
| PRE-{id} | {使用业务语言描述，如"用户已登录"、"购物车非空"} |

##### 校验规则

| 编号 | 校验对象 | 校验条件 | 不符合时提示 |
|------|---------|---------|------------|
| VAL-{id} | {校验对象} | {校验条件} | {用户提示信息} |

##### 触发条件与后置结果

| 触发条件 | 成功后置结果 | 失败后置结果 |
|---------|------------|------------|
| {用户操作/系统事件/定时任务} | {状态变更/通知/后续跳转} | {回滚/错误提示/重试} |

#### 业务流程上下文

| 所属流程 | 流程中的位置 | 上游操作 | 下游操作 |
|---------|------------|---------|---------|
| {流程名称} | {在流程中的步骤位置} | {本功能点之前需要完成的操作} | {本功能点完成后触发的操作} |

#### 功能行为矩阵

| 场景类型 | 场景描述 | 前置条件 | 触发动作 | 预期结果 |
|---------|---------|---------|---------|---------|
| Happy Path (HP) | {正常流程场景} | {前置条件} | {用户操作} | {预期结果} |
| Error Path (EP) | {异常场景1} | {前置条件} | {用户操作} | {错误提示/处理结果} |
| Error Path (EP) | {异常场景2} | {前置条件} | {用户操作} | {错误提示/处理结果} |
| Edge Case (EC) | {边界场景} | {前置条件} | {用户操作} | {预期结果} |

#### 交互约束

| 约束类型 | 约束描述 | 适用场景 |
|---------|---------|---------|
| 输入约束 | {输入字段的格式、长度、范围限制} | {适用场景} |
| 状态约束 | {功能点在特定状态下不可用或行为不同} | {适用场景} |
| 顺序约束 | {必须先完成A再执行B} | {适用场景} |
| 权限约束 | {需要特定角色或权限} | {适用场景} |

---

### FP-{id+1}: {功能点名称}

> 来源变更: {change-name} | 归档时间: {YYYY-MM-DD}

...（同上结构，按 FP-ID 匹配：已存在则替换更新，不存在则追加）

---

## 来源标注汇总

| 功能点ID | 来源变更 | 归档时间 | 说明 |
|----------|---------|---------|------|
| FP-{id} | {change-name} | {YYYY-MM-DD} | {新功能点/替换更新} |

---

## 本册修订记录

| 版本 | 日期 | 修订内容 | 来源变更 |
|------|------|---------|---------|
| 1.0.0 | {YYYY-MM-DD} | 初始版本（由 AI 逆向分析生成） | 系统生成 |

---

> **维护说明**: 本文档由归档阶段自动更新。每次变更归档时，按 FP-ID 匹配：已存在则替换更新对应功能点章节，不存在则追加。首次合并时检测草稿标记（「由 AI 逆向分析生成」），自动替换为正式来源标注。禁止手动编辑。
