---
stage: 编码
skill: kflow-code
version: 1.1.0
created_at: 2026-05-06
template_for: docs/service-guide.md
template_description: 项目服务指引模板，记录项目类型、多环境配置（dev/test/staging/prod）、服务依赖、测试账号和健康检查信息。由 kflow-code 在编码阶段自动生成或检查确认，供编码、服务刷新和测试阶段使用。
---

> **配置状态**: ⏳ 待配置
> **上次检测**: {ISO 8601 时间戳}
> **生成来源**: {kflow-init 逆向分析 / kflow-code 自动分析}

# 项目服务指引

## 项目类型
- **类型**: {Java/Spring Boot | Node.js | Go | Python | 其他}
- **框架**: {框架名称}

## 多环境配置

### dev 环境

| 配置项 | 值 | 说明 |
|--------|---|------|
| 启动命令 | {命令} | |
| 端口 | {端口} | |
| 数据库 | {连接地址} | |
| 配置变量 | `ENV=dev` | |

### test 环境

| 配置项 | 值 | 说明 |
|--------|---|------|
| 启动命令 | {命令} | |
| 端口 | {端口} | |
| 数据库 | {连接地址} | |

### staging 环境

| 配置项 | 值 | 说明 |
|--------|---|------|
| 启动命令 | {命令} | |
| 端口 | {端口} | |
| 数据库 | {连接地址} | |

### prod 环境

| 配置项 | 值 | 说明 |
|--------|---|------|
| 启动命令 | {命令} | |
| 端口 | {端口} | |
| 数据库 | {连接地址} | |

> **安全要求**: 密码、密钥等敏感信息必须通过环境变量引用（如 `${DB_PASSWORD}`），禁止明文写入。

## 服务依赖

识别项目依赖的外部服务（数据库、缓存、消息队列、对象存储等）。

| 服务名称 | 类型 | dev 环境地址 | 端口 | 数据库/实例名 | 配置来源 | 状态 |
|---------|------|-------------|------|-------------|---------|------|
| {服务名} | {数据库/缓存/MQ/对象存储} | {主机地址} | {端口} | {数据库名/实例名} | {配置文件路径} | ⏳ 待确认 |

> 无外部服务依赖时标注「无外部服务依赖」。

## 测试账号

| 环境 | 账号类型 | 用户名 | 密码来源 |
|------|---------|--------|---------|
| dev | 管理员 | {admin} | `${DEV_ADMIN_PASSWORD}` |
| test | 普通用户 | {testuser} | `${TEST_USER_PASSWORD}` |

## 健康检查

| 检查项 | 接口 | 说明 |
|--------|------|------|
| 服务健康 | /health | 服务健康检查接口 |
| 数据库连接 | /db-health | 数据库连接检查接口 |
