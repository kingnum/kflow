# KFlow Skills 体系设计 - 概述

> **版本**: 参见仓库根目录 `VERSION` 文件
> **创建时间**: 2026-04-29
> **更新时间**: 2026-05-17

---

## 一、设计概述

### 1.1 目标

设计一套适用于公司内部开发流程的 Skills 体系，采用文档驱动的阶段门控机制，确保需求从设计到交付的全流程可追溯、可验证。

### 1.2 核心概念

**设计层级重新划分**：

```
变更—— 顶层管理单元（设计在变更级，执行在子变更级）
    │
    ├─▶ 设计探索 (变更级) → 原型设计 (变更级，可选) → 详细设计 (变更级)
    │       │
    │       └─▶ 输出 detailed-design.md（统一详细设计，含 NFR 章节）
    │
    ├─▶ 子变更划分（设计完成后，基于完整设计认知）
    │
    └─▶ 子变更—— 执行单元（计划 → 编码 → 接口单元测试 → E2E测试）
            │                                      功能点 ≤ 10
            │
            └─▶ 按依赖顺序依次完成
                   │
                   ▼
              集成测试 (变更级) → 归档
```

**关键规则**：
- 设计在变更级统一完成，子变更划分后置到详细设计之后
- 子变更职责简化为执行单元：仅负责计划、编码、测试
- 子变更目录包含 .status.md、tasks.md、test-reports/、checkpoints/ 等文件
- 当变更的功能点超过 10 个时，必须拆分为多个子变更
- 每个变更最多 20 个子变更，超过时需拆分为多个变更
- 子变更按依赖关系确定实现顺序，依次完成
- 所有子变更完成后，执行集成测试，然后变更整体归档

### 1.3 核心设计决策

| 决策项 | 选择 | 说明 |
|-------|------|------|
| 粒度区分 | 渐进式流程 | 一个 Skill 内部根据输入自动判断产品级/功能级/缺陷级 |
| 项目类型区分 | 自动检测 | 设计探索阶段检测项目类型，区分前后端项目和纯后端项目 |
| 设计层级 | 设计在变更级，执行在子变更级 | 详细设计提升到变更级，消除"先划分后设计"的矛盾 |
| 前后端项目流程 | 11 阶段 | 设计探索 → 原型设计(可选) → 详细设计 → 计划 → 编码 → 代码审查 → 接口单元测试 → E2E测试 → 集成测试 → 审计(门控) → 归档 |
| 纯后端项目流程 | 9 阶段 | 设计探索 → 详细设计 → 计划 → 编码 → 代码审查 → 接口单元测试 → 集成测试 → 审计(门控) → 归档 |
| 原型设计 | 可选推荐 | 前端变更推荐原型设计，但不强制 |
| 多视角审查 | Agent 并行审查 | 参考 gstack Review Army，四视角在变更级一次审查 |
| 代码审查 | 独立 Skill，两视角并行审查 | `kflow-code-review`：安全+规范、质量+性能，含闭环验证 |
| 阶段回退 | ⚠️ 需修订 状态 + 分级联动 | 需求变更或设计错误触发，分三级联动 |
| 目录位置 | docs/changes/ | 遵循项目文档结构规范 |
| 阶段门控 | 状态文件门控 | 每个阶段完成后更新状态文件 |
| 任务清单 | Markdown checkbox | 在变更目录下创建 tasks.md，功能点级全展开形式 |
| 子变更划分 | 设计完成后基于完整认知划分 | 按业务功能划分，每子变更不超过10个功能点 |
| 子变更上限 | 最多 20 个 | 一个变更最多 20 个子变更，超过时需拆分 |
| 测试时机 | 设计文档先行 | 设计阶段编写测试用例文档，编码阶段编写测试代码 |
| 审查视角 | 四视角并行（设计）+ 两视角并行（代码） | 业务、技术、安全、质量 |
| 审查闭环 | 分级重审 + 追踪矩阵 | 高严重度双视角重审，中严重度原视角重审，低严重度抽查 |
| 状态格式 | Markdown 表格 | 便于人类阅读 |
| 缺陷修复 | 根因分类路由 | 实现错误修复代码 / 测试错误修正测试 / 设计错误触发回退 |
| 归档方式 | 整体归档 | 所有子变更完成 + 集成测试通过后，变更整体归档 |
| 归档条件 | 集成测试 + 全阶段完成 | 集成测试通过 + 所有子变更各阶段完成 |
| NFR 要求 | 设计阶段定义 | detailed-design.md 含性能/安全/可用性/可维护性章节 |
| DoD 验收标准 | 计划阶段定义 | 每功能点 4 维验收（Happy Path + Error Path + Edge Case + Quality） |
| 条件产物引用 | 标准化图例 | ✅ 必须 / 🔶 条件 / ⏭️ 不适用 |
| 数据库迁移 | 变更级管理 | migrations/ 目录，含迁移脚本和回滚脚本 |
| 多环境配置 | 四环境 | service-guide.md 区分 dev/test/staging/prod |
| 跨变更冲突检测 | 编码前检查 | 检查活跃变更文件重叠 |
| 使用评估 | 归档前自动审计 + 手动调用 | 七维度评估（流程合规性、产物完整性、审查质量、测试充分性、缺陷管理、效率指标、Post-mortem汇总） |
| 环境初始化 | 能力发现 + 工具推荐 | 检测 MCP servers 和 Skills，评估组合可行性，输出 toolchain.md |
| 缺陷修复等级 | 三级分离 | `kflow-bug-triage` 四层溯源诊断；`kflow-bug-fix` 负责子变更级二分法；`kflow-integration-test` 内聚变更级四分法 |
| 设计合并 | 归档时合并到产品级 | 功能设计合并到 docs/designs/functional-designs/（前后端：{menu}/index.md + part-NN.md；纯后端：{domain}.md）+ 技术设计分散更新到 technical-designs/ 6 文件（含 config-items.md、error-handling.md），标注来源变更 |
| 文件命名 | 双层目录体系 | functional-designs/（功能设计目录）+ detailed-design.md（详细设计） |
| 文档拆分 | 产品级多文件 + 变更级目录化拆分 | 产品级从一开始多文件拆分（functional-designs 按菜单目录化或按设计域平铺），变更级功能点>30拆分为多文件（index.md + part-NN.md），4 组文档目录化 |
| 用户评审门控 | 原型设计后 + 详细设计后 | 关键设计阶段完成后须用户确认，AskUserQuestion 交互，评审记录持久化 |
| 覆盖追溯矩阵 | traceability.md 独立文件 | 功能点×阶段产物全链路覆盖映射，覆盖率自动验证，< 100% 阻塞阶段门控 |
| 执行类阶段重复制 | 执行类阶段统一重复制 | 计划/编码/审查/测试阶段采用重复制——子代理每轮全量遍历全部工作项，复杂度评估仅信息展示，10 轮自然收敛 + 主 Agent 验收闭环 |
| 变更级服务刷新 | 编码→测试门控同步点 | 所有子变更编码+审查完成后统一编译-迁移-重启-健康检查 |
| checkpoint 两级化 | 变更级 + 子变更级分开存储 | 恢复时优先读子变更级，含过期清理规则 |
| 架构评估自动触发 | 连续3轮同用例失败自动触发 | 自动收集证据 + 多方案分析 + 用户决策，在 `kflow-integration-test` 中实现 |
| 10 轮自审机制 | explore/prototype/design 三阶段强制执行 | 每阶段 10 轮自循环审查，每轮独立记录时间戳文件，覆盖阶段专属维度，不允许提前终止 |
| 阶段边界强制 | 文档白名单 + 标准产物强制 + 越界禁止 | 每阶段仅允许创建输出产物表列出的文件，信息不足记录到 skill-suggestion.md，禁止越界输出 |
| 功能设计升维 | functional-designs/ 扩展为用户体验规格说明书 | 新增页面菜单、可执行操作、表单项定义、业务规则、业务流程闭环，为 prototype 提供精确输入 |
| 审查目录分离 | self-reviews/ + cross-reviews/ | 自审与交叉审查分目录存储，语义明确，时间戳命名自然排序 |

### 1.4 Skills 清单

| Skill 名称 | 阶段 | 归属层级 | 必须性 | 核心功能 |
|-----------|------|---------|--------|---------|
| `kflow-guide` | 流程指引 | — | 按需 | 意图识别、活跃变更检测、跨变更冲突检测 |
| `kflow-explore` | 设计探索 | 变更级 | 必须 | 需求澄清、功能点拆分、项目类型检测（不再划分子变更） |
| `kflow-prototype-design` | 原型设计 | 变更级 | 可选 | HTML 原型设计（委托 huashu-design）、用户评审确认 |
| `kflow-design` | 详细设计 | **变更级** | 必须 | 统一详细设计（含 NFR）、四视角审查、子变更划分、测试用例文档、用户评审确认 |
| `kflow-plan` | 计划 | 子变更级 | 必须 | Checkbox 任务清单、DoD 验收标准、功能点级全展开 |
| `kflow-code` | 编码 | 子变更级 | 必须 | TDD 流程、数据库迁移、跨变更冲突检测、多 Agent 并行编码 |
| `kflow-code-review` | 代码审查 | 子变更级 | 必须 | 两视角并行审查（安全+规范/质量+性能）、闭环验证 |
| `kflow-api-test` | 接口单元测试 | 子变更级 | 必须 | curl/HTTP 接口测试、健康评分、所有项目类型必须执行 |
| `kflow-e2e-test` | E2E测试 | 子变更级 | 前后端必须 | 浏览器自动化测试、仅前后端项目、依赖前置 kflow-api-test |
| `kflow-bug-fix` | 缺陷修复 | 按需 | 按需 | 子变更级二分法根因分类路由、实现/测试错误修复 |
| `kflow-bug-triage` | 问题分诊 | — | 按需 | 四层溯源诊断、问题登记（bugs/）、路由决策 |
| `kflow-integration-test` | 集成测试 | 变更级 | 必须 | 集成测试执行、内聚四分法修复循环、架构评估自动触发 |
| `kflow-status` | 状态总结 | — | 按需 | 未归档变更状态汇总、子变更进度矩阵 |
| `kflow-archive` | 归档 | 变更级 | 必须 | 变更归档、集成测试门控、索引更新、设计合并 |
| `kflow-audit` | 使用评估 | — | 归档门控+按需 | 七维度评估、审计报告、审计回退路由 |
| `kflow-init` | 环境初始化 | — | 按需 | 环境能力发现、工具推荐矩阵、toolchain.md 输出 |
| `kflow-resume` | 中断恢复 | — | 按需 | 变更名定位断点、优先级链读取状态、调度阶段 Skill |

---

## 二、参考资源

### 2.1 参考的 Skills

| 来源 | Skill | 借鉴内容 |
|------|-------|---------|
| superpowers | brainstorming | 硬性门控、清单式任务、流程图可视化 |
| superpowers | test-driven-development | Red-Green-Refactor 循环、任务拆分模板 |
| superpowers | systematic-debugging | 四阶段调试流程 |
| gstack | qa | 多阶段测试流程、健康评分、修复循环 |
| gstack | review | Agent 并行审查（Review Army） |
| openspec | propose/apply/archive | 文档驱动的阶段管理 |
| kflow-skills | task-management | 任务清单创建、状态追踪、归档机制 |
| kflow-skills | bug-fix | 缺陷修复闭环流程 |

### 2.2 关键设计模式

| 模式 | 来源 | 应用 |
|------|------|------|
| 状态文件门控 | openspec | 每阶段完成后更新 .status.md |
| Checkbox 任务追踪 | openspec + kflow | tasks.md 格式 |
| Agent 并行审查 | gstack | 四视角（设计）+ 两视角（代码）并行审查 |
| TDD 流程 | superpowers | 编码阶段核心流程 |
| 渐进式流程 | 新设计 | 自动判断变更粒度 |
| 阶段回退机制 | 新设计 | ⚠️ 需修订 状态 + 分级联动回退 |
| 根因分类路由 | 新设计 | kflow-bug-triage 四层溯源 + kflow-bug-fix 二分法 + 变更级四级分类 |
| 使用评估审计 | 新设计 | 归档前自动触发七维度审计，问题归属阶段回退 |
| 环境能力发现 | 新设计 | MCP + Skills 双重感知，工具推荐矩阵 |
| 设计合并归档 | 新设计 | 功能设计按菜单/设计域合并到产品级 + 技术设计按 6 文件体系分散更新 |
| 文档拆分策略 | 新设计 | 产品级多文件拆分 + 变更级条件拆分 |
| 覆盖追溯矩阵 | 新设计 | FP×阶段产物 全链路覆盖映射，覆盖率自动验证门控 |
| 执行类阶段重复制 | 新设计 | 执行类阶段统一重复制——子代理每轮全量遍历，10 轮自然收敛 + 主 Agent 验收 |

---

## 三、实施计划

### 3.1 实施顺序

建议按以下顺序实现各 Skill：

| 序号 | Skill | 原因 |
|------|-------|------|
| 1 | kflow-guide | 入口 Skill，意图识别和流程引导（含 RESUME 路由、NEW CHANGE 指引） |
| 2 | kflow-explore | 设计探索入口，项目类型检测（不再划分子变更） |
| 3 | kflow-design | 核心 Skill，变更级统一设计 + 审查（依赖 kflow-explore） |
| 4 | kflow-plan | 子变更级任务计划，依赖变更级 detailed-design.md |
| 5 | kflow-code | TDD 流程 + 多 Agent 并行编码，依赖 kflow-plan |
| 6 | kflow-code-review | 两视角并行代码审查，依赖 kflow-code |
| 7 | kflow-api-test | 子变更接口单元测试，所有项目类型，依赖 kflow-code-review |
| 8 | kflow-e2e-test | 子变更 E2E 测试，仅前后端项目，依赖 kflow-api-test |
| 9 | kflow-bug-fix | 子变更级二分法根因分类路由，实现/测试错误修复 |
| 9.5 | kflow-bug-triage | 独立诊断，四层溯源定位问题源头，路由到 REVISION 或 bug-fix |
| 10 | kflow-integration-test | 变更级集成测试 + 四分法修复循环 + 架构评估自动触发 |
| 11 | kflow-prototype-design | 可选阶段，委托 huashu-design Skill |
| 12 | kflow-status | 状态汇总，支持子变更进度矩阵 |
| 13 | kflow-resume | 中断恢复，通过变更名定位断点并调度阶段 Skill |
| 14 | kflow-archive | 归档流程，集成测试门控，设计合并 |
| 15 | kflow-audit | 使用评估，七维度审计，归档门控集成 |
| 16 | kflow-init | 环境初始化，能力发现，工具链推荐，CLAUDE.md 注入 |

### 3.1.1 跨域机制集成

以下跨域机制在相关 Skill 实现时同步集成，不作为独立 Skill：

| 机制 | 集成目标 Skill | 说明 |
|------|---------------|------|
| 覆盖追溯矩阵 (traceability.md) | kflow-design（创建）、所有执行类阶段（填写）、kflow-archive（归档验证） | 功能点×阶段产物全链路覆盖映射，覆盖率门控自动验证 |
| 执行类阶段重复制 | kflow-plan、kflow-code、kflow-code-review、kflow-api-test、kflow-e2e-test、kflow-bug-fix、kflow-integration-test | 复杂度评估（仅信息展示）→ Agent 子代理调度 → 每轮全量遍历 → 主 Agent 验收闭环 |

### 3.2 依赖关系

```
Skill 实现依赖:

kflow-guide ───────────────────────────────────────────────────────────
    │
    ├─▶ kflow-explore ──────────────────────────────────────────────────
    │       │
    │       ├─▶ kflow-prototype-design (可选，仅前后端项目) ──▶ 👤用户评审
    │       │       │
    │       └───────┴─▶ kflow-design (变更级，统一设计 + 划分子变更) ──▶ 👤用户评审
    │                           │
    │                           ├─▶ kflow-plan (子变更级，输入变更级 detailed-design.md)
    │                           │       │
    │                           │       └─▶ kflow-code (子变更级)
    │                           │               │
    │                           │               └─▶ kflow-code-review (子变更级，代码审查)
    │                           │                       │
    │                           │                       ├─▶ kflow-api-test (子变更级，接口单元测试)
    │                           │                       │       │
    │                           │                       │       └─▶ kflow-e2e-test (子变更级，仅前后端)
    │                           │                       │               │
    │                           │                       │               └─▶ kflow-bug-fix (按需，子变更级)
    │                           │                       │
    │                           │                       └─▶ kflow-integration-test (变更级，所有子变更完成后)
    │                           │
    │                           │                               │
    │                           │                               └─▶ kflow-audit (归档门控)
    │                           └─▶ (设计错误回退路由)
    │
    ├─▶ kflow-status (独立，可随时调用)
    │
    ├─▶ kflow-audit (归档门控 + 按需手动调用)
    │
    ├─▶ kflow-init (独立，项目启动时调用)
    │
    ├─▶ kflow-resume (独立，通过 kflow-guide 路由，中断恢复)
    │
    └─▶ kflow-archive (归档阶段，集成测试通过后，含设计合并)
```
